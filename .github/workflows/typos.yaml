name: Check for typos

on: [pull_request]

jobs:
  check-for-typos:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v5

      - name: Check for typos
        uses: crate-ci/typos@a67079b4ae32e18c3f53d75368c52ce53b5fb56b
