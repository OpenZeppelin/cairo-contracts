name: Check for typos

on: [pull_request]

jobs:
  check-for-typos:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v5

      - name: Check for typos
        uses: crate-ci/typos@65f69f021b736bdbe548ce72200500752d42b40e
